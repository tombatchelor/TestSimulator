version: v1.0
name: Java
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Maven
    task:
      jobs:
        - name: Build
          commands:
            - checkout
            - sem-version java 1.14
            - mvn -B package --file pom.xml clean install test
      epilogue:
        always:
          commands:
            - ls -ltr
